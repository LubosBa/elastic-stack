output {

    #elasticsearch {
    #    hosts => [ "els1" ]
    #    user => "logstash_write"
    #    password => "velkykokokot12340!"
    #}

    stdout { codec => rubydebug { metadata => true } }
}