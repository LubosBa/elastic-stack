input {
    kafka {

        bootstrap_servers => "kafs1:9092"
        client_id => "logs1.m8sk"
        consumer_threads => 1
        decorate_events => true
        security_protocol => "PLAINTEXT"

        topics => ["test" ]
    }
}